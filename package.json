{
  "name": "FreshProject",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "clear-watchman": "watchman watch-del-all",
    "start": "yarn clear-watchman && react-native start --reset-cache",
    "fresh": "rm -rf yarn.lock && rm -rf node_modules && cd ios && rm -rf Podfile.lock && pod deintegrate && cd .. && yarn && cd ios && pod install --repo-update && cd ..",
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "test": "jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "clevertap-react-native": "^1.0.2",
    "react": "18.1.0",
    "react-native": "0.70.7"
  },
  "devDependencies": {
    "@babel/core": "^7.12.9",
    "@babel/runtime": "^7.12.5",
    "@react-native-community/eslint-config": "^2.0.0",
    "babel-jest": "^26.6.3",
    "eslint": "^7.32.0",
    "jest": "^26.6.3",
    "metro-react-native-babel-preset": "0.72.3",
    "react-test-renderer": "18.1.0"
  },
  "jest": {
    "preset": "react-native"
  }
}
